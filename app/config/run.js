(function() {
	'use strict';

	/**
	 * @name gn-test
	 * @description
	 *
	 * Run phase of the Application.
	 */
	angular
		.module('gn-test')
		.run(run);

	function run($state, $rootScope) {

	}
})();
